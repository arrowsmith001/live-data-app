import { Dashboard, Schema, SettingsInputAntenna } from "@mui/icons-material"

export const ConnectionIcon = () => {
    return <SettingsInputAntenna sx={{margin: 1}}/>;
}

export const DashboardIcon = () => {
    return <Dashboard sx={{margin: 1}}/>;
}

export const SchemaIcon = () => {
    return <Schema sx={{margin: 1}}/>;
}