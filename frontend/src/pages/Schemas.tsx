import { useState } from "react";


const Schemas = () => {
    const [selected, setSelected] = useState("Schemas");
    return (
        <div>
            <h1>Schemas</h1>
        </div>
    );
}

export default Schemas;